<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <!--
    <item
        android:id="@+id/action_example"
        android:title="@string/action_example"
        app:showAsAction="withText|ifRoom" />
    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="never" />
    -->

    <!--
	ifRoom 会显示在Item中，但是如果已经有4个或者4个以上的Item时会隐藏在溢出列表中。当然个数并不仅仅局限于4个，依据屏幕的宽窄而定
	never 永远不会显示。只会在溢出列表中显示，而且只显示标题，所以在定义item的时候，最好把标题都带上。
	always 无论是否溢出，总会显示。
	withText withText值示意Action bar要显示文本标题。Action bar会尽可能的显示这个标题，但是，如果图标有效并且受到Action bar空间的限制，文本标题有可能显示不全。
	collapseActionView 声明了这个操作视窗应该被折叠到一个按钮中，当用户选择这个按钮时，这个操作视窗展开。否则，这个操作视窗在默认的情况下是可见的，并且即便在用于不适用的时候，也要占据操作栏的有效空间。一般要配合ifRoom一起使用才会有效果。
	-->

    <!-- android:actionViewClass="android.widget.SearchView" -->
    <!--
    <item
        android:id="@+id/menu_item_search"
        android:icon="@drawable/abc_ic_search_api_mtrl_alpha"
        android:title="搜索"
        android:actionViewClass="android.widget.SearchView"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView"/>
    -->

    <!-- android:actionProviderClass="android.widget.ShareActionProvider" -->
    <!--
    <item
        android:id="@+id/menu_item_share"
        android:icon="@drawable/ic_compose"
        android:title="分享"
        android:actionProviderClass="android.widget.ShareActionProvider"
        xcd:actionProviderClass="android.support.v7.widget.ShareActionProvider"
        xcd:showAsAction="always"/>
    -->

    <!--
    app:actionProviderClass="android.support.v7.widget.ShareActionProvider"
    app:actionProviderClass="net.quduo.pixel.interfaces.android.common.NavigationActionProvider"
    -->

    <!--
    <item
        android:id="@+id/menu_search"
        android:icon="@drawable/abc_ic_search_api_mtrl_alpha"
        android:title="搜索"
        app:showAsAction="always" />

    <item
        android:id="@+id/menu_more"
        android:icon="@drawable/abc_ic_go_search_api_mtrl_alpha"
        android:title="更多"
        app:showAsAction="always" />
    -->
    <!--
    <item
        android:id="@+id/menu_navigation"
        android:title="导航"
        app:actionProviderClass="net.quduo.pixel.interfaces.android.common.MenuActionProvider"
        app:showAsAction="always" />
    -->
</menu>
